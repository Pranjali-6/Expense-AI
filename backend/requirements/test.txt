# Test tooling only. Layered on top of api.txt or worker.txt via the
# INSTALL_TEST build arg, so neither image carries the other's dependencies
# just to run tests.

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
faker==33.0.0
freezegun==1.5.1
ruff==0.8.1
mypy==1.13.0
